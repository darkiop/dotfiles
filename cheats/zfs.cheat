% zfs

# list filesystems/volumes
zfs list

# list snapshots
zfs list -t snapshot

# create a snapshot
zfs snapshot <pools>@<snapshot-name>

# rollback a snapshot
zfs rollback rpool/<snapshots>

# destroy a snapshot
zfs destroy rpool/<snapshots>

# delete all snapshots
zfs destroy rpool@%

# list all snapshot
zfs list -H -o name -t snapshot

$ pools: zfs list | awk '{print $1}' | tail -n +2
$ snapshots: zfs list -H -o name -t snapshot