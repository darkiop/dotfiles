% tmux

# start tmux
tmux

# list tmux sessions
tmux ls

# re-attach a session
tmux a -t <tmuxsessions>

# kill a session
tmux kill-session -t <tmuxsessions>

$ tmuxsessions: tmux ls -F "#{session_name}"